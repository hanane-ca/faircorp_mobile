<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emse.faircorpmobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaircorpMobile">
        <activity
            android:name=".RoomActivity"
            android:exported="true" />
        <activity
            android:name=".RoomsActivity"
            android:exported="true" />
        <activity
            android:name=".WindowsActivity"
            android:exported="true" />
        <activity
            android:name=".BasicActivity"
            android:exported="true"
            android:label="@string/title_activity_basic"
            android:theme="@style/Theme.FaircorpMobile.NoActionBar" />
        <activity
            android:name=".WindowActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>